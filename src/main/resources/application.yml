spring:
  profiles: local
  datasource:
    url: jdbc:mysql://localhost:3306/gametime?reconnect=true
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto:  create     #update   validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect